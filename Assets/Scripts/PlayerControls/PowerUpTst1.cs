using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PowerUpTst1 : PowerUpManager
{

    // method called on all objects of the class, on the GameObject.
    public override void onPowerupActivate()
    {
        
    }

    // Start is fcalled before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
